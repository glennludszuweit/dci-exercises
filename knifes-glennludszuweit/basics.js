// Create a variable called hello containing the string "Hello"
let hello = 'Hello';

// Create a variable called world containing the string "World"
let world = 'World';

// Create a variable called helloWorld combining the two Strings
// with a space inbetween them
let helloWorld = `${hello} ${world}`;

// Create a funcion called sayHelloWorld that returns
// the variable helloWorld
function sayHelloWorld() {
  return helloWorld;
}

sayHelloWorld();
